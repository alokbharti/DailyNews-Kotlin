<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/secondary_text_dark</item>
        <item name="colorPrimaryDark">@android:color/secondary_text_dark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorOnSecondary">@color/grayLight</item>
    </style>

    <!--<style name="DarkMode" parent="@style/Theme.AppCompat.DayNight">
        <item name="android:forceDarkAllowed">true</item>
    </style>-->

</resources>
